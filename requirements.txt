# DEPRECATED single requirements file.
#
# CURE and MAIRA-2 need conflicting transformers versions, so dependencies are split
# per model and installed into separate venvs by scripts/run_vast.sh:
#
#   requirements-base.txt     shared deps (no transformers, no bitsandbytes)
#   requirements-maira2.txt   -> .venv-maira2  (transformers==4.51.3)
#   requirements-cure.txt     -> .venv-cure    (transformers==4.55.4 + peft==0.17.1)
#
# Just run:  DATA_DIR=/data HF_TOKEN=hf_xxx ./scripts/run_vast.sh
